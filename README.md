# Vectors-reading-
This program demonstrates the use of vectors in C++, which are dynamic arrays that can grow or shrink in size during program execution. Unlike normal arrays that require a fixed size, vectors allow us to store any number of elements without knowing the size beforehand. In this program, I used vector&lt;int> to store students’ marks. 
